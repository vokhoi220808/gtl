/* PDF Fusion Smart Pro - Trusted Verify Registry UI v9.0.0 Auto Register
   Registry-backed authenticity checks + automatic registration after QR Verify export. */
(function(){
  'use strict';
  const STORE_SECRET = 'pfspVerifyAdminSecretSession';
  const LOCAL_AUDIT = 'pfspTrustedVerifyAudit';
  const LOCAL_REGISTRY = 'pfspTrustedVerifyLocalRecords';
  const API = './api/verify';
  const $ = id => document.getElementById(id);

  function html(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
  function short(h){return String(h||'').slice(0,12).toUpperCase();}
  function now(){return new Date().toISOString();}
  function hasHashVerify(){return !!(window.PFSPHashVerify && typeof window.PFSPHashVerify.getLastCertificate === 'function');}
  function getCert(){try{return hasHashVerify()?window.PFSPHashVerify.getLastCertificate():null;}catch{return null;}}
  function saveAudit(event){try{const list=JSON.parse(localStorage.getItem(LOCAL_AUDIT)||'[]');list.unshift({time:now(),...event});localStorage.setItem(LOCAL_AUDIT,JSON.stringify(list.slice(0,120)));}catch{}}
  function getAudit(){try{return JSON.parse(localStorage.getItem(LOCAL_AUDIT)||'[]');}catch{return[];}}
  function getSecret(){try{return sessionStorage.getItem(STORE_SECRET)||'';}catch{return '';}}
  function setSecret(v){try{if(v)sessionStorage.setItem(STORE_SECRET,v);else sessionStorage.removeItem(STORE_SECRET);}catch{}}
  function localRecords(){try{return JSON.parse(localStorage.getItem(LOCAL_REGISTRY)||'[]');}catch{return[];}}
  function saveLocalRecord(record){try{const list=localRecords().filter(r=>String(r.id).toUpperCase()!==String(record.id).toUpperCase());list.unshift({...record,localOnly:true,registeredAt:record.registeredAt||now(),status:record.status||'active'});localStorage.setItem(LOCAL_REGISTRY,JSON.stringify(list.slice(0,250)));return true;}catch{return false;}}
  function certToRecord(cert){if(!cert)return null;return {id:String(cert.id||'').toUpperCase(),sha256:String(cert.sha256||cert.h||'').toLowerCase(),size:cert.size,fileName:cert.fileName||'document.pdf',mimeType:cert.mimeType||'application/pdf',createdAt:cert.createdAt||cert.ts||now(),origin:cert.origin||location.origin,app:cert.app||'PDF Fusion Smart Pro',appVersion:cert.appVersion||'unknown',sourceBundleSha256:cert.sourceBundleSha256||cert.sh||'',checksum:cert.checksum||cert.hck||'',status:'active',registeredBy:cert.registeredBy||'PDF Fusion Smart Pro UI'};}
  function currentStage(){const cert=getCert();const reg=cert&&cert.__registryResult;return {cert,reg};}

  async function staticRegistryCheck(record){try{const r=await fetch('./data/verify-registry.json',{cache:'no-store'});if(!r.ok)return null;const reg=await r.json();const found=(reg.records||[]).find(x=>String(x.id).toUpperCase()===String(record.id).toUpperCase());if(!found)return null;if(record.sha256&&String(found.sha256||'').toLowerCase()!==String(record.sha256).toLowerCase())return {ok:false,verdict:'FAKE_OR_MODIFIED',level:'bad',message:'ID có trong registry tĩnh nhưng SHA-256 không khớp.',record:found};if(String(found.status||'active').toLowerCase()==='revoked')return {ok:false,verdict:'REVOKED',level:'bad',message:'ID đã bị thu hồi trong registry tĩnh.',record:found};if(found.expiresAt&&Date.parse(found.expiresAt)&&Date.parse(found.expiresAt)<Date.now())return {ok:false,verdict:'EXPIRED',level:'bad',message:'ID đã hết hạn trong registry tĩnh.',record:found};return {ok:true,verdict:record.sha256?'GENUINE':'REGISTERED_NEEDS_FILE',level:record.sha256?'good':'warn',message:'Khớp registry tĩnh data/verify-registry.json.',record:found};}catch{return null;}}

  async function apiCheck(recordOrCert){const record=recordOrCert&&recordOrCert.sha256?recordOrCert:certToRecord(recordOrCert);if(!record||!record.id)throw new Error('Chưa có Verify ID/certificate để kiểm tra.');const qs=new URLSearchParams({id:record.id});if(record.sha256)qs.set('sha256',record.sha256);if(record.size!=null)qs.set('size',String(record.size));let payload=null;try{const r=await fetch(API+'?'+qs.toString(),{cache:'no-store'});payload=await r.json();if(payload&&payload.verdict&&payload.verdict!=='UNKNOWN')return payload;}catch(err){}const staticResult=await staticRegistryCheck(record);if(staticResult)return staticResult;const local=localRecords().find(x=>String(x.id).toUpperCase()===String(record.id).toUpperCase());if(local){if(record.sha256&&String(local.sha256).toLowerCase()!==String(record.sha256).toLowerCase())return {ok:false,verdict:'FAKE_OR_MODIFIED',level:'bad',message:'ID có trong registry cục bộ nhưng SHA-256 không khớp.',record:local};return {ok:true,verdict:record.sha256?'GENUINE':'REGISTERED_NEEDS_FILE',level:record.sha256?'good':'warn',message:'Khớp registry cục bộ trên trình duyệt này. Lưu ý: cục bộ không phải registry công khai.',record:local};}return payload||{ok:false,verdict:'UNKNOWN',level:'warn',message:'Không kết nối được registry hoặc ID chưa đăng ký.'};}
  async function apiRegister(cert,opts){const record=certToRecord(cert);if(!record||!record.id||!record.sha256)throw new Error('Cần xuất PDF để có certificate SHA-256 trước.');const secret=(opts&&opts.secret)||getSecret();if(secret)setSecret(secret);const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','X-Verify-Admin-Secret':secret},body:JSON.stringify({action:'register',certificate:record,adminSecret:secret,overwrite:!!(opts&&opts.overwrite)})});const payload=await r.json().catch(()=>({verdict:'SERVER_ERROR',message:'Không đọc được phản hồi server.'}));saveAudit({action:'register',id:record.id,verdict:payload.verdict,ok:payload.ok});return payload;}
  async function autoRegisterCertificate(cert){const record=certToRecord(cert);if(!record||!record.id||!record.sha256)throw new Error('Auto register cần certificate có ID và SHA-256.');record.registeredBy='auto-qr-export';const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','X-PFSP-Auto-Register':'1'},body:JSON.stringify({action:'auto-register',certificate:record,registeredBy:'auto-qr-export',note:'Auto registered when user exported a PDF with QR Verify.'})});const payload=await r.json().catch(()=>({ok:false,verdict:'SERVER_ERROR',message:'Không đọc được phản hồi server.'}));saveAudit({action:'auto-register',id:record.id,verdict:payload.verdict,ok:payload.ok});try{cert.__registryResult=payload;localStorage.setItem('pfspDocumentHashLastCertificate',JSON.stringify(cert));}catch{}if(payload&&payload.ok&&payload.record)saveLocalRecord(payload.record);updateAutoRegisterMini(payload);return payload;}
  async function apiRevoke(id,reason,secret){const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','X-Verify-Admin-Secret':secret||getSecret()},body:JSON.stringify({action:'revoke',id,reason,adminSecret:secret||getSecret()})});const payload=await r.json().catch(()=>({verdict:'SERVER_ERROR',message:'Không đọc được phản hồi server.'}));saveAudit({action:'revoke',id,verdict:payload.verdict,ok:payload.ok});return payload;}
  async function copyText(text,ok){try{await navigator.clipboard.writeText(text);if(ok)ok();}catch{prompt('Copy thủ công:',text);}}

  function verdictLabel(v){return {GENUINE:'✅ THẬT - khớp registry',REGISTERED:'✅ Đã đăng ký thủ công',AUTO_REGISTERED:'✅ Tự đăng ký thành công',ALREADY_REGISTERED:'✅ Đã có trong registry',REGISTERED_NEEDS_FILE:'⚠️ ID có registry, cần upload PDF',UNKNOWN:'⚠️ Chưa có trong registry',FAKE_OR_MODIFIED:'❌ GIẢ MẠO hoặc đã sửa',REVOKED:'❌ ID đã bị thu hồi',EXPIRED:'❌ ID đã hết hạn',REGISTRY_TAMPERED:'❌ Registry có dấu hiệu bị sửa',AUTO_REGISTER_PENDING_MANUAL_PATCH:'⚠️ Cần patch thủ công',MANUAL_REGISTRY_PATCH_REQUIRED:'⚠️ Cần commit registry thủ công',MANUAL_REVOKE_PATCH_REQUIRED:'⚠️ Cần commit patch thu hồi',UNAUTHORIZED:'❌ Sai admin secret',BAD_HASH:'❌ Hash không hợp lệ',BAD_ID:'❌ Verify ID không hợp lệ',ID_COLLISION:'❌ Trùng ID khác hash',AUTO_REGISTER_DISABLED:'⚠️ Auto register bị tắt',SERVER_ERROR:'❌ Lỗi server'}[v]||v||'UNKNOWN';}
  function levelOf(payload){return payload&&payload.level||(payload&&payload.ok?'good':'warn');}
  function classOf(level){return level==='good'?'good':level==='bad'?'bad':'warn';}
  function renderStatus(box,payload){if(!box)return;const v=payload&&payload.verdict||'UNKNOWN';const cls=classOf(levelOf(payload));const record=payload&&payload.record||payload&&payload.existing;box.className='pfsp-tv-result '+cls;box.innerHTML=`<div class="pfsp-tv-verdict"><span>${html(verdictLabel(v))}</span><small>${html(payload&&payload.message||'')}</small></div>`+(record?`<div class="pfsp-tv-kv"><span>ID</span><code>${html(record.id)}</code><span>SHA-256</span><code>${html(short(record.sha256))}…</code><span>Trạng thái</span><code>${html(record.status||'active')}</code><span>Đăng ký</span><code>${html(record.registeredAt||'-')}</code><span>Nguồn</span><code>${html(record.registrationMode||record.registeredBy||'-')}</code></div>`:'')+(payload&&payload.commit?`<p class="pfsp-tv-link"><a href="${html(payload.commit)}" target="_blank" rel="noopener">Mở commit registry</a></p>`:'');}
  function updateAutoRegisterMini(payload){const box=$('pfspTvAutoMini');if(!box)return;const cls=classOf(levelOf(payload));box.className='pfsp-tv-auto-mini '+cls;box.innerHTML=`<b>${html(verdictLabel(payload&&payload.verdict))}</b><span>${html(payload&&payload.message||'')}</span>`;}
  function renderAudit(){const box=$('pfspTvAudit');if(!box)return;const list=getAudit().slice(0,8);box.innerHTML=list.length?list.map(x=>`<div><b>${html(x.action)}</b> <code>${html(x.id||'-')}</code> <span>${html(x.verdict||'')}</span><small>${html(x.time||'')}</small></div>`).join(''):'<p>Chưa có audit cục bộ.</p>';}
  function renderReadiness(){const box=$('pfspTvReady');if(!box)return;const cert=getCert();const qrOn=!!$('qrEnable')?.checked&&(($('qrMode')?.value||'verify')==='verify');const hashOn=!!$('qrDocHashEnable')?.checked;const autoOn=!$('qrHashAutoRegister')||$('qrHashAutoRegister').checked!==false;const steps=[['QR Verify',qrOn],['SHA-256',hashOn],['Auto Register',autoOn],['Certificate',!!cert]];box.innerHTML=steps.map(([label,on])=>`<span class="pfsp-tv-chip ${on?'on':'off'}">${on?'✓':'•'} ${html(label)}</span>`).join('');}

  function injectStyles(){if($('pfspTrustedVerifyStyles'))return;const style=document.createElement('style');style.id='pfspTrustedVerifyStyles';style.textContent=`
    .pfsp-tv-panel{margin-top:18px;padding:18px;border:1px solid rgba(56,189,248,.35);border-radius:26px;background:radial-gradient(circle at 10% 0%,rgba(34,211,238,.18),transparent 32%),linear-gradient(145deg,rgba(2,6,23,.92),rgba(15,23,42,.88) 55%,rgba(30,41,59,.92));box-shadow:0 28px 80px rgba(2,6,23,.38),inset 0 1px 0 rgba(255,255,255,.06);color:#e5edff}.pfsp-tv-head{display:flex;gap:14px;align-items:flex-start;justify-content:space-between;flex-wrap:wrap}.pfsp-tv-title{display:flex;gap:12px;align-items:center}.pfsp-tv-icon{width:44px;height:44px;display:grid;place-items:center;border-radius:16px;background:linear-gradient(135deg,#22c55e,#06b6d4);box-shadow:0 12px 30px rgba(34,211,238,.22)}.pfsp-tv-panel h4{margin:0;color:#f8fafc;font-size:18px}.pfsp-tv-panel p{margin:6px 0;color:#cbd5e1}.pfsp-tv-small{font-size:12px;max-width:760px}.pfsp-tv-ready{display:flex;gap:7px;flex-wrap:wrap;margin:12px 0}.pfsp-tv-chip{border:1px solid rgba(148,163,184,.28);border-radius:999px;padding:6px 9px;font-size:12px;font-weight:800}.pfsp-tv-chip.on{color:#bbf7d0;background:rgba(22,101,52,.34);border-color:rgba(34,197,94,.38)}.pfsp-tv-chip.off{color:#cbd5e1;background:rgba(15,23,42,.72)}.pfsp-tv-actions{display:flex;flex-wrap:wrap;gap:9px;margin-top:12px}.pfsp-tv-actions button,.pfsp-tv-actions input{border-radius:14px;border:1px solid rgba(148,163,184,.32);padding:10px 12px}.pfsp-tv-actions input{min-width:250px;background:rgba(2,6,23,.68);color:#f8fafc}.pfsp-tv-actions input::placeholder{color:#94a3b8}.pfsp-tv-actions button{background:rgba(30,41,59,.88);color:#f8fafc;font-weight:850;cursor:pointer}.pfsp-tv-actions button:hover{background:rgba(8,47,73,.92);border-color:rgba(34,211,238,.46);transform:translateY(-1px)}.pfsp-tv-actions .primary{background:linear-gradient(135deg,#2563eb,#06b6d4);border-color:rgba(125,211,252,.45)}.pfsp-tv-actions .danger{background:rgba(127,29,29,.55);border-color:rgba(248,113,113,.42)}.pfsp-tv-auto-mini{margin-top:12px;padding:12px 14px;border-radius:16px;border:1px solid rgba(148,163,184,.26);background:rgba(2,6,23,.54);display:grid;gap:4px}.pfsp-tv-auto-mini.good{border-color:rgba(34,197,94,.55);background:rgba(20,83,45,.34);color:#bbf7d0}.pfsp-tv-auto-mini.warn{border-color:rgba(245,158,11,.55);background:rgba(120,53,15,.30);color:#fde68a}.pfsp-tv-auto-mini.bad{border-color:rgba(248,113,113,.64);background:rgba(127,29,29,.34);color:#fecaca}.pfsp-tv-result{margin-top:12px;padding:16px;border-radius:20px;border:1px solid rgba(148,163,184,.28);background:rgba(15,23,42,.78);color:#e5edff}.pfsp-tv-result.good{border-color:rgba(34,197,94,.60);background:linear-gradient(135deg,rgba(20,83,45,.45),rgba(15,23,42,.86))}.pfsp-tv-result.warn{border-color:rgba(245,158,11,.62);background:linear-gradient(135deg,rgba(120,53,15,.38),rgba(15,23,42,.86))}.pfsp-tv-result.bad{border-color:rgba(248,113,113,.72);background:linear-gradient(135deg,rgba(127,29,29,.46),rgba(15,23,42,.90))}.pfsp-tv-verdict{display:grid;gap:5px}.pfsp-tv-verdict span{font-size:18px;font-weight:900;color:#f8fafc}.pfsp-tv-verdict small{color:#cbd5e1}.pfsp-tv-kv{display:grid;grid-template-columns:110px minmax(0,1fr);gap:7px 10px;margin-top:12px}.pfsp-tv-kv span{color:#cbd5e1;font-weight:850}.pfsp-tv-kv code{word-break:break-all;color:#f8fafc;background:rgba(2,6,23,.48);border:1px solid rgba(148,163,184,.20);border-radius:9px;padding:3px 7px}.pfsp-tv-textarea{width:100%;min-height:118px;margin-top:12px;border-radius:16px;border:1px solid rgba(148,163,184,.32);padding:11px;font-family:ui-monospace,monospace;font-size:12px;background:rgba(2,6,23,.62);color:#e5edff}.pfsp-tv-audit{margin-top:12px;display:grid;gap:6px}.pfsp-tv-audit div{display:grid;grid-template-columns:110px minmax(0,1fr) 160px;gap:8px;align-items:center;border:1px solid rgba(148,163,184,.18);border-radius:12px;padding:8px;background:rgba(2,6,23,.38);font-size:12px}.pfsp-tv-audit code{word-break:break-all}.pfsp-tv-audit small{color:#94a3b8}@media(max-width:720px){.pfsp-tv-audit div,.pfsp-tv-kv{grid-template-columns:1fr}.pfsp-tv-actions input{min-width:100%}}`;
    document.head.appendChild(style);}
  function injectMainPanel(){if($('pfspTrustedVerifyPanel'))return;const target=$('qrDocHashPanel')||$('qrVerifyBox')||$('tools')||document.querySelector('main');if(!target)return;const div=document.createElement('div');div.id='pfspTrustedVerifyPanel';div.className='pfsp-tv-panel';div.innerHTML=`<div class="pfsp-tv-head"><div class="pfsp-tv-title"><div class="pfsp-tv-icon">🛡</div><div><h4>Trusted Verify Registry Pro</h4><p class="pfsp-tv-small">Tự đăng ký mỗi khi xuất PDF có QR Verify. Registry lưu ID + SHA-256 để phân biệt <b>thật / giả mạo / đã sửa / thu hồi</b>. Link hash không còn là nguồn tin duy nhất.</p></div></div><a class="btn" href="./verify-registry.html" target="_blank" rel="noopener">Tìm hiểu</a></div><div id="pfspTvReady" class="pfsp-tv-ready"></div><div id="pfspTvAutoMini" class="pfsp-tv-auto-mini warn"><b>Auto Register đang chờ</b><span>Bật QR Verify rồi xuất PDF. Hệ thống sẽ tự đăng ký vào registry qua API.</span></div><div class="pfsp-tv-actions"><input id="pfspTvSecret" type="password" autocomplete="off" placeholder="Admin secret chỉ dùng khi thu hồi/đăng ký thủ công" value="${html(getSecret())}"><button type="button" id="pfspTvCheck" class="primary">Kiểm tra certificate cuối</button><button type="button" id="pfspTvRegister">Đăng ký thủ công</button><button type="button" id="pfspTvCopyPatch">Copy registry patch</button><button type="button" id="pfspTvLocal">Lưu cục bộ</button><button type="button" id="pfspTvRevoke" class="danger">Thu hồi ID</button></div><div id="pfspTvResult" class="pfsp-tv-result warn"><div class="pfsp-tv-verdict"><span>Chưa kiểm tra registry</span><small>Hãy xuất PDF có QR Verify để hệ thống tự đăng ký, hoặc bấm kiểm tra certificate cuối.</small></div></div><textarea id="pfspTvPatch" class="pfsp-tv-textarea" readonly placeholder="Registry patch/manual JSON sẽ hiện ở đây nếu backend chưa ghi được GitHub."></textarea><div id="pfspTvAudit" class="pfsp-tv-audit"></div>`;target.parentNode.insertBefore(div,target.nextSibling);const result=$('pfspTvResult');$('pfspTvSecret').addEventListener('change',e=>setSecret(e.target.value.trim()));$('pfspTvCheck').onclick=async()=>{try{result.textContent='Đang kiểm tra registry...';renderStatus(result,await apiCheck(getCert()));}catch(err){renderStatus(result,{verdict:'SERVER_ERROR',level:'bad',message:err.message||String(err)});}};$('pfspTvRegister').onclick=async()=>{try{result.textContent='Đang đăng ký registry...';const payload=await apiRegister(getCert(),{secret:$('pfspTvSecret').value.trim()});renderStatus(result,payload);if(payload.suggestedRegistry)$('pfspTvPatch').value=JSON.stringify(payload.suggestedRegistry,null,2);renderAudit();}catch(err){renderStatus(result,{verdict:'SERVER_ERROR',level:'bad',message:err.message||String(err)});}};$('pfspTvCopyPatch').onclick=async()=>{const cert=getCert();const rec=certToRecord(cert);if(!rec){renderStatus(result,{verdict:'BAD_ID',level:'bad',message:'Chưa có certificate để tạo patch.'});return;}const patch={version:'PFSP-VERIFY-REGISTRY-v1',updatedAt:now(),records:[rec]};$('pfspTvPatch').value=JSON.stringify(patch,null,2);await copyText($('pfspTvPatch').value,()=>renderStatus(result,{verdict:'MANUAL_REGISTRY_PATCH_REQUIRED',level:'warn',message:'Đã copy patch. Dán record này vào data/verify-registry.json nếu backend chưa ghi được GitHub.'}));};$('pfspTvLocal').onclick=()=>{const rec=certToRecord(getCert());if(!rec){renderStatus(result,{verdict:'BAD_ID',level:'bad',message:'Chưa có certificate để lưu cục bộ.'});return;}saveLocalRecord(rec);renderStatus(result,{verdict:'REGISTERED',level:'good',message:'Đã lưu vào registry cục bộ của trình duyệt này. Chỉ dùng để test.',record:rec});};$('pfspTvRevoke').onclick=async()=>{const cert=getCert();const id=cert&&cert.id||prompt('Nhập Verify ID cần thu hồi:');if(!id)return;try{result.textContent='Đang thu hồi...';const payload=await apiRevoke(id,prompt('Lý do thu hồi:','PDF không còn hợp lệ')||'revoked',$('pfspTvSecret').value.trim());renderStatus(result,payload);if(payload.suggestedRegistry)$('pfspTvPatch').value=JSON.stringify(payload.suggestedRegistry,null,2);renderAudit();}catch(err){renderStatus(result,{verdict:'SERVER_ERROR',level:'bad',message:err.message||String(err)});}};document.addEventListener('pfsp:auto-register-result',ev=>{const payload=ev.detail||{};renderStatus(result,payload);if(payload.suggestedRegistry)$('pfspTvPatch').value=JSON.stringify(payload.suggestedRegistry,null,2);renderAudit();});['qrEnable','qrMode','qrDocHashEnable','qrHashAutoRegister'].forEach(id=>$(id)?.addEventListener('change',renderReadiness));renderReadiness();renderAudit();}
  function init(){injectStyles();injectMainPanel();}
  window.PFSPTrustedVerifyRegistry={apiCheck,apiRegister,autoRegisterCertificate,apiRevoke,saveLocalRecord,localRecords,certToRecord,renderStatus,updateAutoRegisterMini};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else setTimeout(init,0);
})();
